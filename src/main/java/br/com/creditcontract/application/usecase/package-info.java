/**
 * Application layer — use cases orchestrate the domain.
 *
 * <p>Each use case class follows the Single Responsibility Principle:
 * one class, one use case. They depend only on domain ports (interfaces),
 * never on infrastructure adapters.
 */
package br.com.creditcontract.application.usecase;
