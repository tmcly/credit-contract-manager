/**
 * Output ports (interfaces) for external dependencies of the domain.
 *
 * <p>Every external system the credit-contract domain needs to talk to
 * is declared as an interface here, with a technology-agnostic contract.
 * Infrastructure implements these ports with concrete adapters.
 */
package br.com.creditcontract.domain.port;
