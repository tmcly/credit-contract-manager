/**
 * Data Transfer Objects for the REST layer.
 *
 * <p>DTOs decouple the HTTP contract from the domain model — the
 * response shape can evolve independently of the aggregate.
 */
package br.com.creditcontract.interfaces.rest.dto;
