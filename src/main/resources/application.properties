spring.application.name=contrato-credito

# Porta da aplicação
server.port=8080

# Actuator: expõe o healthcheck nativo do Spring Boot
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Endpoint custom de saúde (adicional ao actuator)
# GET /health -> { "status": "UP" }
